rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function signedIn() { return request.auth != null; }
    // Basic production baseline: authenticated shop staff only.
    match /{document=**} {
      allow read, write: if signedIn();
    }
  }
}
