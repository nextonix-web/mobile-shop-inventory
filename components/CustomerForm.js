import { useEffect, useState } from 'react';
const empty={name:'',phone:'',address:'',totalReceivable:0};
export default function CustomerForm({initial,onSubmit,onCancel}){const [f,setF]=useState(empty);useEffect(()=>setF(initial||empty),[initial]);return <form className="card" onSubmit={e=>{e.preventDefault();onSubmit({name:f.name.trim(),phone:f.phone.trim(),address:f.address.trim(),totalReceivable:Number(f.totalReceivable||0)})}}><h2>{initial?'Edit':'Add'} Customer</h2><div className="form-grid"><div className="field"><label>Name</label><input required value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></div><div className="field"><label>Phone</label><input value={f.phone} onChange={e=>setF({...f,phone:e.target.value})}/></div><div className="field"><label>Opening Receivable</label><input type="number" value={f.totalReceivable||0} onChange={e=>setF({...f,totalReceivable:e.target.value})}/></div><div className="field"><label>Address</label><input value={f.address} onChange={e=>setF({...f,address:e.target.value})}/></div></div><div className="row" style={{marginTop:12}}><button className="btn primary">Save</button>{onCancel&&<button type="button" className="btn muted" onClick={onCancel}>Cancel</button>}</div></form>}
